---
import GameCard from './GameCard.astro';

const games = [
  {
    title: "Guns & Fury",
    genre: "Action · Western · Mobile",
    description: "Fast-paced 1v1 duels, bounty missions, and endless zombie waves. Master the art of the Wild West.",
    image: "/img/games/guns-fury.webp",
    links: [
      { platform: "App Store", url: "https://apps.apple.com/us/app/guns-fury/id1589304660" },
    ]
  },
];
---

<section id="games" class="section">
  <div class="container">
    <!-- Section Header -->
    <div class="text-center mb-12 reveal">
      <span class="section-label">Our Work</span>
      <h2 class="section-title">Games</h2>
      <p class="text-warm-400 max-w-2xl mx-auto">
        Discover the games we've crafted with passion and dedication.
      </p>
    </div>

    <!-- Games -->
    <div class="flex justify-center reveal" style="transition-delay: 150ms;">
      <div class="w-full max-w-md">
        {games.map((game) => (
          <GameCard {...game} />
        ))}
      </div>
    </div>

    <!-- Empty State (if no games) -->
    {games.length === 0 && (
      <div class="text-center py-16">
        <p class="text-warm-400 text-lg">
          Exciting games coming soon! Stay tuned.
        </p>
      </div>
    )}
  </div>
</section>
